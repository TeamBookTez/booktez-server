//parse options
var destinationIP = options.destinationIP || null;
var shouldSecureTransfer = options.shouldSecureTransfer || null;
    var password = options.password || null;
    
//if should secure, check for password
    if (shouldSecureTransfer){
      if (!password){
        console.log('Transfer should be secure but no password provided');
        process.exit(2);
      }
    }